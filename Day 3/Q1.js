let ip = +prompt('Enter the Number');
let op = ip%2 === 0 ? 'Even' : 'Odd';
console.log(`The Number entered is ${ip} and Number is ${op}`);

/*      let ip = prompt("Enter a Number: ");
        let op;
        if(ip % 2 == 0)
        {
            op = "Even";
        }
        else{
            op = "Odd";
        }
        console.log(`The Number entered is ${ip} and Number is ${op}`);*/
        